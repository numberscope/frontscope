import {describe} from 'vitest'
// import {mount} from '@vue/test-utils'
// import BundleCard from '../BundleCard.vue'

describe.todo('BundleCard', () => {
    /*
    If we try to import BundleCard, we get an error when we do
    `pnpm test:unit`:

    TypeError: document.hasFocus is not a function
    ❯ Object.254../constants node_modules/p5/lib/p5.min.js:3:401058

    The test suite isn't providing the document object. I'll leave this file 
    as a placeholder. Maybe we'll figure out a way to test this in the future.

    The dead code below is what we should test if/when we can import BundleCard.
    */
    // it('Draw button emits drawBundle when clicked', () => {
    //     const wrapper = mount(BundleCard)
    //     wrapper.findAll('a')[0].trigger('click')
    //     expect(wrapper.emitted()).toHaveProperty('drawBundle')
    // })
    // it('Remove button emits removeBundle when clicked', () => {
    //     const wrapper = mount(BundleCard)
    //     wrapper.findAll('a')[1].trigger('click')
    //     expect(wrapper.emitted()).toHaveProperty('removeBundle')
    // })
})
